for i in range(110234, 300124):
    if i ** 0.5 % 1 == 0:
        # if int(i**0.5) == (i **0.5) - или так
        # проверяем полный квадрат потому что кол-во делителей НЕЧЕТНОЕ
        divisors = []
        for j in range(1, i + 1):
            if i % j == 0:
                divisors.append(j)
        if len(divisors) == 5:
            print(i, divisors)
# Если просят НЕЧЕТНОЕ кол-во делителей, можно перебирать только числа из которых извл корень
# Если ЧЕТНОЕ - то наоборот